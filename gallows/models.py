from django.db import models


""" class Game(models.Model):
    DIFFICULTIES = [
        ('e', 'Easy'),
        ('n', 'Normal'),
        ('h', 'Hard'),
    ]

    word = models.CharField(max_length=20)
    difficulty = models.ChoiceField(choices=DIFFICULTIES)
    counter = models.IntegerField()
    guess = CharField(max_length=1)
    
    class Meta:
        pass

    def __str__(self):
        pass
 """
